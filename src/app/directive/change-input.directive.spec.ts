import { ChangeInputDirective } from './change-input.directive';

describe('ChangeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeInputDirective();
    expect(directive).toBeTruthy();
  });
});
